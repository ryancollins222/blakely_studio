section#artists
  .row
    .col
      h1 Blakely Studio
      h2 Artists
  .row
    each artist in artists
      .col.col-9.col-lg-4.artist-col
        .artist-container
          a(href= artist.url)
            h3= artist.name
            .artist-pic(style=`background-image:url(${artist.img});`)